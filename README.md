# Sierra Android SDK

Please refer to the SDK documentation that has been provided to you.

The SDK is distributed through [JitPack](https://jitpack.io/#ai.sierra/sierra-android-sdk). Depend
on the specific per-module coordinate(s) your app needs (for example
`ai.sierra.sierra-android-sdk:sierra-android-sdk` for chat) rather than the aggregate coordinate.
